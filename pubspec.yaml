name: cqrs_offline_sync
description: Command-based offline-first sync runtime primitives for Dart apps.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.9.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  meta: ^1.12.0
  args: ^2.5.0
  path: ^1.9.1
  yaml: ^3.1.2
  glob: ^2.1.2

executables:
  cqrs_sync:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
